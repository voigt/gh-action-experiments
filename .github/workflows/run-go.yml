on:
  workflow_dispatch:

jobs:
  experiment:
    runs-on: ubuntu-latest
    steps:
      - name: "Go Version"
        run: sudo go version
      - name: "Install Go"
        run: sudo apt update && sudoapt install -y golang
      - name: "Execute Go Program"
        shell: go run {0}
        run: |
          package main
          import "fmt"
          func main() {
              fmt.Println("hello world")
          }
