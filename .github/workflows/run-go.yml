on:
  workflow_dispatch:

jobs:
  experiment:
    runs-on: ubuntu-latest
    steps:
      - name: "Go Version"
        run: sudo go version
      - name: "Execute Go Program"
        env:
          GO111MODULE: off
          GOPATH: "."
        shell: go run {0}
        run: |
          package main
          import "fmt"
          func main() {
              fmt.Println("hello world")
          }
